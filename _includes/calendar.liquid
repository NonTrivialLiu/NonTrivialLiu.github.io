{% if include.calendar_id %}
  <div class="post mt-4">
    <h2 class="mb-3"><i class="fas fa-calendar-alt"></i> {{ 'calendar.upcoming_events' | t }}</h2>

    <button
      class="btn btn-sm btn-outline-primary mb-3 calendar-toggle-btn"
      type="button"
      onclick="toggleCalendar()"
      id="calendar-toggle-btn"
      aria-pressed="false"
    >
      {{ 'calendar.show_calendar' | t }}
    </button>

    <div class="calendar-wrapper" id="calendar-container" style="display: none;">
      <iframe
        id="calendar-iframe"
        data-calendar-id="{{ include.calendar_id }}"
        data-timezone="{{ include.timezone | default: 'UTC' }}"
        style="{{ include.style | default: 'border:0; width:100%; height:600px;' }}"
        frameborder="0"
        scrolling="no"
      >
      </iframe>
    </div>
  </div>
{% endif %}
